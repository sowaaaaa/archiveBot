services:
  archivebot:
    image: ${DOCKER_IMAGE:-archivebot:latest}
    env_file: .env
    volumes:
      - data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  data:
